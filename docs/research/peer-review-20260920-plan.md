# Checklist and plan peer review — 2026-09-20

Scope: FEATURES.md, observation-session-design.md, continuation handoff and trial evidence. Primary review plus an independent read-only agent review requested by the user. Documentation reconciliation only; no implementation/build/install or new app test occurred.

| Finding | Resolution |
|---|---|
| Immediate CAM-01–12 order conflicted with capture-first direction. | PLAN.md now owns REC-00–03 and remaining CAM-03–12 sequence; no repeat of completed campaign solely due document refresh. |
| KEY-04 claimed TESTED for secondary action families supported only by scroll evidence. | Downgraded to IMPLEMENTED; scroll pass retained separately. |
| SCROLL index and FLOW-02 omitted latest focus/autonomous recovery results. | Updated to the scoped native passes; historical reports retained. |
| Editor report said build/install required although candidate was installed. | Corrected and linked exact installed SHA-256 and install record; native verification remains pending. |
| Outcome rows overlapped and obscured the Save failure. | RUN-04 records the demonstrated gap, RUN-AXPRESS is IMPLEMENTED/unverified, RUN-14 references DATA-01's proposed structured contract. |
| Internal polling/rendered-baseline concerns were hidden beneath generic diff TESTED claim. | OBS-07 is RECHECK with specific remaining cases; DATA-03 owns baseline/query work. |
| Storage design lacked individually testable acceptance cases. | DATA-08–10 and PLAN foundation gates cover root binding, exclusion, read-only opens, concurrency, schema handling, recording failure and completeness. |
| No clear policy for journal failure before/after dispatch. | Before intent commit failure: refuse input. After dispatch: preserve sent/uncertain, stop dependent steps, never replay to repair missing evidence. |
| Event-capture proposal lacked a decisive temporal fixture. | REC-03 requires transient notification retrieval while an action blocks; timestamps distinguish receipt from later attribute reads. |
| TUI P0 rows appeared mixed with immediate foundation work. | Added DEFERRED status and separate console section; design section ordering corrected. |
| Old continuation had obsolete installed path, `/tmp` work, live-agent claims and false Sky-no-synthesis assertion. | Archived as superseded history; replaced with current concise handoff and authoritative plan links. |

Remaining uncertainty is explicit: no native pass yet for installed AXPress guard, no durable recorder implementation, no new changed-route persistence pass, no Simulator campaign or Blender artifacts, no general certification. Current scoped scroll passes remain valid historical evidence; exact pixel-distance parity is not claimed.

Validation: document diff whitespace checks and relative-link/feature-ID checks, not application tests. See PLAN.md for next execution. Architecture recommendations are not marked IMPLEMENTED merely because their requirements are now specified.
