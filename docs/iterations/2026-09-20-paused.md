# 2026-09-20 — Desktop checkpoint and user pause

## Outcome and evidence

User paused the project and requested removal of Leap MCP from this app and Codex. Removed global `leap` registration with `codex mcp remove leap`; `codex mcp get leap` confirms absent. Current host is Codex desktop. No separate Leap MCP registration was found in inspected ChatGPT preferences/support configuration. Existing loaded tools may remain until host restart. Claude registration, source, installed binary, skills and runtime history were not removed.

Native desktop session 06655764-718E-42FA-B313-E10D8569D932 confirmed insights_enabled=true. PASS and 2X2 SLOTS assertions passed; Sideline/play selection/information navigation worked. Duplicated play as LEAP DESKTOP RECHECK 0920 - Slant Flat. Exact note Read flat defender. Halftime adjustment: attack inside leverage. was typed and read back (4015). Save returned AX -25204 but left editor. Initial reopening selected the original record, so its failed note assertion was not a persistence failure for the duplicate. Reset filters, searched exact new title, selected duplicate, reopened; title and note assertions both passed in F62AA8AE-C118-4977-BE25-03A338C65EAF. Zoom/reset worked; broad zoom selector initially ambiguous because disabled background preview controls also exist. Sidebar scroll interaction7180BC27-53ED-48D0-B82A-FC0B50EFA26B detected scrollbar0→0.4806687565308255 and visual change. Raw evidence local-only in .leap/sessions/<session>/.

## Rationale and alternatives

Honor explicit pause immediately instead of proceeding with proposed fixes or further acceptance. Desktop outcome is a scoped pass with observation and AX acknowledgement rough edges, not general Sky parity.

## Changes

Removed Codex MCP configuration only. No implementation changes. Proposed fix for observation errors incorrectly relabeling acknowledged input dispatch as uncertain was NOT started. No rebuild/install.

## Validation and delivery

CLI removal succeeded and absence verified. Existing installed binary remains7faa53f candidate. Project documentation checkpoint committed; all runtime records preserved.

## Remaining work

Project PAUSED at user request. Do not resume testing or reinstall MCP without user direction. If resumed: fix/report phase distinction in AutomationEngine catch, investigate post-typing observation timeout, retain uncertain AX Save acknowledgement without replay. Simulator typing deferred; remaining Simulator clicking and Blender gates incomplete.
